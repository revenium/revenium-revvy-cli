# Revenium API key — required when running with --non-interactive,
# otherwise the CLI will prompt for it interactively.
REVENIUM_METERING_API_KEY=

# Override the Revenium API base URL. Only needed for dev/staging
# environments; the public CLI defaults to production.
# REVENIUM_METERING_BASE_URL=
